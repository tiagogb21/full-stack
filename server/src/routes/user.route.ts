import * as express from 'express';

const router = express.Router();

// Criando CRUD

// Create:
router.post('', async () => {});

// Read:
router.get('', async () => {});

// Update:
router.put('', async () => {});

// Delete:
router.delete('', async () => {});
